CREATE TABLE node_notes (node_id TEXT PRIMARY KEY REFERENCES nodes(id) ON DELETE CASCADE, notes TEXT NOT NULL DEFAULT '', version INTEGER NOT NULL DEFAULT 1, updated INTEGER NOT NULL, updated_by TEXT NOT NULL);
