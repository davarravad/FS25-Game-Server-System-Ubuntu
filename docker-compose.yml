services:
  web:
    build:
      context: .
      dockerfile: docker/php/Dockerfile
    container_name: fsg-panel-web
    restart: unless-stopped
    environment:
      APP_NAME: ${APP_NAME}
      APP_ENV: ${APP_ENV}
      APP_URL: ${APP_URL}
      APP_KEY: ${APP_KEY}
      SESSION_SECRET: ${SESSION_SECRET}
      DB_HOST: ${DB_HOST}
      DB_PORT: ${DB_PORT}
      DB_NAME: ${DB_NAME}
      DB_USER: ${DB_USER}
      DB_PASSWORD: ${DB_PASSWORD}
      AGENT_URL: ${AGENT_URL}
      AGENT_SHARED_TOKEN: ${AGENT_SHARED_TOKEN}
      SHARED_GAME_PATH: ${SHARED_GAME_PATH}
      SHARED_DLC_PATH: ${SHARED_DLC_PATH}
      SHARED_INSTALLER_PATH: ${SHARED_INSTALLER_PATH}
      ADMIN_DEFAULT_USERNAME: ${ADMIN_DEFAULT_USERNAME}
      ADMIN_DEFAULT_PASSWORD: ${ADMIN_DEFAULT_PASSWORD}
      INSTANCE_BASE_PATH: ${INSTANCE_BASE_PATH}
      BACKUP_BASE_PATH: ${BACKUP_BASE_PATH}
      TZ: ${TZ}
    volumes:
      - ./app/web:/var/www/html
    depends_on:
      - db
      - agent
    networks:
      - panel

  nginx:
    image: nginx:1.27-alpine
    container_name: fsg-panel-nginx
    restart: unless-stopped
    ports:
      - "${PANEL_PORT}:80"
    volumes:
      - ./app/web:/var/www/html:ro
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - web
    networks:
      - panel

  db:
    image: mariadb:11.8
    container_name: fsg-panel-db
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_ROOT_PASSWORD}
      MYSQL_DATABASE: ${DB_NAME}
      MYSQL_USER: ${DB_USER}
      MYSQL_PASSWORD: ${DB_PASSWORD}
      TZ: ${TZ}
    volumes:
      - db_data:/var/lib/mysql
      - ./sql/init:/docker-entrypoint-initdb.d:ro
    networks:
      - panel

  agent:
    build:
      context: .
      dockerfile: docker/agent/Dockerfile
    container_name: fsg-panel-agent
    restart: unless-stopped
    environment:
      AGENT_SHARED_TOKEN: ${AGENT_SHARED_TOKEN}
      INSTANCE_BASE_PATH: ${INSTANCE_BASE_PATH}
      BACKUP_BASE_PATH: ${BACKUP_BASE_PATH}
      TZ: ${TZ}
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ${INSTANCE_BASE_PATH}:${INSTANCE_BASE_PATH}
      - ${BACKUP_BASE_PATH}:${BACKUP_BASE_PATH}
      - ./templates:/app/templates:ro
      - ./scripts:/app/scripts:ro
    networks:
      - panel

volumes:
  db_data:

networks:
  panel:
    driver: bridge
